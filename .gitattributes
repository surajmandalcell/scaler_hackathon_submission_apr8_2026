# Keep line endings normalized. No git-lfs filters: this repo is
# text-only by policy (see .gitignore binary-files section). HF Spaces'
# pre-receive hook rejects git-lfs blobs for binary files and requires
# Xet LFS instead — we avoid the issue entirely by never committing
# binaries.
* text=auto eol=lf
