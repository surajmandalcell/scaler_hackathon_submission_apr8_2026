{
  "name": "fundlens",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "//": "Canonical entry point is the Makefile at repo root. The scripts below are kept only for frontend-workspace pass-through (`npm run dev -w packages/frontend` etc.)."
}
